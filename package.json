{
  "name": "agentic-config-visualizer",
  "version": "1.0.0",
  "description": "Visualize per-editor agentic coding configuration (skills, commands, hooks, agents, MCP servers) for a project.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/server.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/wicg-file-system-access": "^2023.10.5"
  }
}
